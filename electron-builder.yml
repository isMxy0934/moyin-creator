appId: com.manju2026.mumu-creator
productName: MUMU
directories:
  buildResources: build
  output: release
files:
  - out/**/*

extraResources:
  - from: demo-data
    to: demo-data
    filter:
      - "**/*"

asar: true
asarUnpack:
  - "**/*.node"
  - "**/node_modules/sharp/**"

# 图标配置
icon: build/icon.ico

win:
  executableName: mumu-creator
  icon: build/icon.ico
  forceCodeSigning: false
  target:
    - target: nsis
      arch:
        - x64

# NSIS 安装包配置
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: MUMU
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  createStartMenuShortcut: true
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
